# Night shift 4b — 14/15 Sep 2026, 22:00 → 02:00: the four visible fails, then the proofs

Written by the coordinator after checking every night-4 capture against Daniel's rules. Night 4's report
(`design/progress/2026-09-15-night4.md`) is honest about its not-done list, but four things in its own captures
fail a standing order and Daniel will see them in the first ten minutes. Those come first. Nothing new is started
until they are fixed and re-captured from the SAME poses.

Daniel's last words tonight: "keep pumping all night. do a good job please." He is back at 02:00–03:00.

## Hard rules (unchanged — a failed rule fails the item)
- Machine clock only: `date` before every phase. Never estimate elapsed time. Hard stops below.
- ONE heavy process at a time: stop the play stack (`C:\dev\StopMythicEarth.bat`) before the editor; close the editor
  before the play stack. Delete `C:\dev\MMOKitEval\Saved\Autosaves` before each editor launch.
- Every terrain edit is a surgical rule on top of the LIVE heightmap through the proven night-4 route
  (`Tools/dalriata/n4/gen_terrain_n4.py` → `n4_01_height.py` import with `force_layers_full_update` → delta re-seat
  of instances and actors → `Landscape.BuildNanite`). Lissban's knoll and NE hill are Daniel-approved: masked out.
  Re-run the sightline + landform audits offline BEFORE any import.
- Never invent a place name or a person's name. The lake stays "the lake". NPC names come only from
  `Content/Data/npcs/dal-riata-npcs.json`.
- Never `git add -A`; explicit paths only. Never commit vendor packs, `Content/ThreeRealms/Props/CelticHouse`,
  `Content/Stylized_Tree_Pack`, or `.f32/.npy/full-size .png` arrays. `pull --rebase` the design repo; never force-push.
- Nothing counts without a capture or a log line. Blockers: time-box 20 min, write them down, move on.
- Report faithfully: what does not work is written as plainly as what does.

## THE FOUR VISIBLE FAILS (in this order; re-capture each from night 4's pose in `Tools/dalriata/n4/poses.json`)

### 1. The river (`river_close_s.jpg`) — "rivers real and flowing", "never turquoise"  (≤ 75 min)
What the capture shows: a bright teal blob lying ON a flat meadow, grass poking up through the water, straight
quad edges visible, no bank, no bed. It reads as a puddle painted on grass.
Do:
- Cut a real CHANNEL into the terrain along all six reaches (`n4_river.json`): Owenfinn bed 1.4–2.0 m below the
  local bank line, channel = ribbon width + a 3 m bank each side at ≤ 1:1.5, width noise-broken at 30/120 m so no
  edge is straight; the five burns 0.5–0.8 m deep with 1.5 m banks. Hold the bed to 0.3 m for ±15 m at every road
  crossing (fords) and taper to nothing at the lake outflow and the mouth. Mask the settlements.
- Re-seat the ribbon quads at bank − 0.5 m (the water sits visibly IN the channel with 0.5 m of bank showing —
  the lake-flooding-the-forest complaint applies here too). Remove every foliage/ground-life instance inside the
  channel + 1.5 m. Re-seat the bank dressing (boulders, reeds, ferns, logs) onto the new bank tops and toes.
- Colour: `MI_TRW_River` must read as the same dark Atlantic grey-green as the lake fix (`n4_23_lakecolour.py`);
  prove it in the CLIENT later, not only in the editor. Foam only at drops.
- Captures: the night-4 `river_close` and `river_bend` poses again, plus one NEW eye-level pose standing on the
  bank looking upstream. All three go in the report side by side with the night-4 versions.

### 2. The waterfall (`waterfall_s.jpg`)  (≤ 40 min)
What the capture shows: two disconnected flat panels floating in front of a black-green cliff, a gap between them,
the cliff face grass-green not rock.
Do:
- Re-chain the fall at 1 m steps with each quad overlapping the next by 40 % (no gaps at any angle), pushed out
  ≤ 0.3 m along the local normal, whitewater material with foam, the plunge pool a little wider, the mist volume kept.
- Hide every panel edge with rock: boulders along both edges of the sheet and across the lip, a boulder apron at the foot.
- The cliff itself must read as rock: zero the painted dirt/duff mask channels on slopes > 45° island-wide so the
  auto-material's rock rule shows through on every cliff, not only here.
- Capture from the night-4 `waterfall` pose and one from the plunge pool looking up.

### 3. The forest crown from below (`forest_lookup_s.jpg`, `forest_interior_s.jpg`)  (≤ 60 min)
What the captures show: looking up from the ground the canopy is a lattice of BARE TWIGS with almost no needles — it
reads as a dead forest. The floor under it is plain flat dark green with almost nothing on it, which is exactly
"plain flat smooth earth floor". The trunks are all one pale grey.
Do, in this order:
- Diagnose before touching anything: the editor's low-spec profile runs `foliage.LODDistanceScale 0.3`, which
  forces the fir's far LODs (fewer needle cards) at close range. Take the look-up pose at 0.3 and at 1.0 and compare.
  If the crown fills in at 1.0, the fix is the LOD screen sizes on `SM_TR_Fir_Canopy/_Emergent/_Sub` (LOD0 held to
  ~60 m, LOD1 to ~150 m) so the CLIENT profile (`sg.FoliageQuality 0`) shows a full crown; measure fps after.
  If it is still bare at 1.0, the needle mask (`M_TR_FirNeedle_Masked`, threshold 13 m ± 2.5 over a 3.2 m band) is
  clipping too much of the crown — check the LocalPosition scale on the three meshes and re-tune until the crown
  is a dense needle roof from the ground. Either way, PROVE IT IN THE CLIENT at the end (a look-up capture in-game).
- Floor clutter at eye level: inside the forest polygon, within any 30 m of a player there must be visible ferns,
  moss patches, fallen branches, a log or a rock — measure it (instances per 100 m² of forest floor, before/after)
  and re-capture the `forest_interior` pose. Vary trunk tint per instance (the pale lavender-grey reads as one clone).

### 4. Underwater (`lake_under_s.jpg`)  (≤ 30 min)
What the capture shows: a featureless teal void — no bed, no surface, nothing to look at. Daniel asked for an
INTENTIONAL underwater view.
Do: cut the lake's LocalFogVolume extinction until the bed is visible to 12–18 m; dress the lake bed (cobbles,
boulders, sunken logs, dark sand patches, reed stems in the shallows) within 40 m of the shore; a visible surface
from below. Capture 2.4 m under looking along the bed toward the shore, and one looking up at the surface.

## THEN, IN THIS ORDER, AS THE CLOCK ALLOWS (editor hard stop 00:50)
5. **Shore and forest-floor textures WITHOUT the landscape material** (≤ 50 min). The sampler budget is proven
   dead (night 3 tried shared samplers `n3_76` and trimming `n3_77`; both failed) — do NOT retry it. Instead: an
   HISM of 8 m ground-patch planes carrying their own material (DBuffer decal blend mode preferred; masked
   soft-edged opaque as the fallback), seated 3 cm over the ground: shingle/wet sand along the coast ramp mask
   (the four strands sandier), needle-duff + moss + roots under the canopy from the duff mask. Noise-broken edges,
   no straight lines. Capture `coast_camasmore` and `coast_shingle_w` again; if it costs more than 3 fps in the
   client, cull it at 300 m.
6. **The hall's open sides** (≤ 30 min): wattle wall segments (`woven_fence_exp1_SM`, or the stone
   `fort_palisade_stone_wall*` if they fit the posts better) in a ring between the hall's posts, doorway left open,
   hearth light kept. Capture `hall_inside` again and one from outside.
7. **The neon tufts**: the near-white, lit-from-inside grass clumps visible in `forest_glade`, `coast_camasmore`
   and `ridge_kingshill` (probably the "dry stalks" kind in `gen_ground_n4.py`). Find which kind it is and tone it
   (tint/desaturate or swap the mesh). 10 min.
8. **Villager bodies** (≤ 30 min, only if 5–7 are done): every villager and the two rest-stop merchants are
   white `BasicShapes/Cylinder` placeholders, in the editor AND in the client (the mannequin in
   `ingame_09_nameplate_fixed.jpg` is Celtictest, not Nest Goch). Replace each cylinder with a `SkeletalMeshActor`
   using the kit mannequin (`/Game/MMOKit/MMO_Assets/Character_UE5_Manny/Meshes/SKM_Manny` +
   `.../Animations/Idle` as a looping single node, like the pen pig) at the same spot and yaw. The two merchants
   keep their plates; give them roster rows ONLY if the JSON already has unplaced merchant/pedlar rows.
9. NOT tonight: the mill (no mill or wheel mesh exists in any pack — say so in the report), UDS, Norway Spruce,
   the 2 m regen, the other villages, the castle.

## PLAY STACK PROOFS (00:55 → 01:35; editor CLOSED first; `C:\dev\PlayMythicEarth.bat`)
- Water colour in the client: the river and the lake from the shore — dark grey-green, not teal.
- The crown from below in the client (item 3), the forest floor at eye level, the shore (item 5).
- **XP fill**: kill one deer (Tab, 1/2/3) — capture the gold fill above 0 and the "Awarded ... Total XP" log line.
- **Player death**: fight a wolf until Celtictest dies — capture the HP at 0, the `PLAYER DIED` log line, and the
  resurrect at LB_Graveyard 8 s later. If the death path does not fire, that is a headline finding — log exactly
  what the kit did instead. (Move Celtictest with `Tools/frostmarch/m3/set_spawn.py` while PersistenceServer is
  stopped; the grind-camp wolves are `DRV2_GrindSpawn_grind_wolves_0/1`.)
- **fps headroom**: `t.MaxFPS 0` for 60 s at Lissban's west gate; median + min from the `[TRMove][CLIENT]` counters.
- No white cylinder anywhere in the client if item 8 was done; otherwise say they remain.
- HAND-OFF: Celtictest back on the track outside Lissban's WEST gate facing the gate (the night-4 spot
  `(−350872, −108288, 4120) uu, yaw 67.5`), spawn written to the DB while PersistenceServer is stopped, stack up,
  a final capture of that view.

## REPORT by 01:50 (stack up by 02:00)
`design/progress/2026-09-15-night4b.md`, captures in `design/progress/captures/2026-09-15-night4b/` (small `_s.jpg`
only; full-size stays local). Per item: what changed, the before/after captures named, the numbers, what does not
work. Both repos committed with explicit paths and pushed; `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` (the shift runs as Opus).
Update the STATE AT HAND-OFF section of memory `three-realms-next-session.md` honestly. Final message: a short
summary with capture names, the proofs' results, fps, and both commit hashes.
