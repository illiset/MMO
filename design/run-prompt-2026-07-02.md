# Autonomous day-run prompt — 2026-07-02

Paste this (or just say "run the day-run prompt from the design repo"):

---

Work autonomously all day on the MMO (invisible mode — never open a visible
window; headless builds and verification only). Work through the milestones
below IN ORDER; commit and push after each meaningful step; log every design
call you make in `design/decisions-log.md`; keep a punch list of everything
needing my eyes in `design/punchlist.md` for tonight's visible session. Don't
wait for me or ask questions — make sensible calls consistent with the design
repo and log them. No purchases, nothing posted publicly, don't modify my
Downloads spreadsheet.

**P0 — Kit runs end to end.** MMO Kit eval project builds on UE 5.7;
persistence server (CodeSpartan, C#) running locally with SQLite; dedicated
server boots; headless client connects, account creation and login verified
through logs. Done when: a scripted headless client can create an account,
log in, create a character, and enter a world.

**P1 — Three Realms identity in the creation flow.** Login → create account
→ pick realm (Great North / Mystic Lands / Honorguard, from our design data)
→ pick CLASS TYPE (archetype: Frontline / Healers / Support / DPS) → name →
enter world. Class types only at creation — actual classes are earned later
(see P4). DAoC-style flow and a loading screen. Realm choice can be visual-
only beyond Great North for now (GN is the playable realm; ML/HG grayed
"coming soon" is fine).

**P2 — Design pipeline feeds the kit.** Extend tools/ so xlsx→JSON now also
generates MMO Kit data assets (abilities, items, classes/creatures as
applicable) or the cleanest kit-native data path. The spreadsheet remains the
single design cockpit. Regenerate + validate must stay one command each.

**P3 — Skills & skill trees (Great North, breadth as data + depth for a
few).** Data model:每 class type has a skill tree; each of GN's 25 classes
has a tree targeting ~20 skills. Generate ALL trees/skills as structured
data with names/costs/cooldowns (derive sensibly from archetype + stats;
mark autogenerated ones "draft" for my review), but implement deeply (real
effects, working in combat) for: Knight (Frontline), Squire (Healers), Bard
(Support), Ranger (DPS) — the slice four. Shield Slam ports in as a Knight
skill. Skill tree UI panel: classic MMORPG look (talent-tree style: tiers,
point spend, prerequisites), functional for the slice four.

**P4 — Class quest at level 10 (prototype).** Playing a class type levels
1-9; at level 10 a quest unlocks choosing/earning a TRUE class from that
type's list (limited by race×class matrix from the design data). Build the
basic version: for a Frontline character at 10, a simple quest (talk to
trainer NPC → kill N training targets → return) that grants the Knight
class, its skill tree, and a title message. Structure it so more class
quests are data, not code.

**P5 — Classic MMORPG UI skin pass.** Inventory, equipment, character/stat
screen, and the creation/login flow styled classic-MMO (DAoC/old-WoW feel:
dark panels, gold accents, serif-ish headers — consistent, not fancy). Stat
screen shows our 16 stats by category. Use kit widgets as the base; keep
everything functional.

Ground rules: UE 5.7 kit project (C:\dev\MMOKitEval) is now the primary
engine project. Prefer C++/data extensions over Blueprint edits where
possible; when Blueprint edits are unavoidable, do them via headless editor
Python if possible and note any that need GUI work on the punch list. Verify
every milestone headlessly (logs, offscreen screenshots, automation tests)
and record proof in the decisions log. If blocked hard on something, skip
it, log it, and continue down the list. End the day by pushing everything,
updating the roadmap, and writing tonight's punch list.

---

## Notes recorded from Daniel (2026-07-01 evening)

- Class types at creation = archetypes; TRUE class earned ~level 10 via
  quest, options limited by class type + race matrix. (New core decision.)
- Target ~20 skills per class long-term; skill trees per class type AND per
  class, unique per realm.
- Great North first; other realms follow the proven loop.
- "Classic looking" inventory/stat/creation screens — DAoC-era soul.
